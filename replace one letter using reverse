void main(){
  String text = 'banana';
  String result = '';
  bool flag = true;
  for(int i=0;i<text.length;i++){
    if(text[i]=='a' && !flag){
      result += '*';
      
    }
    else{
      result += text[i];
      if(text[i]=='a'){
        flag = false;
      }
    }
  }
  print(result);
}
