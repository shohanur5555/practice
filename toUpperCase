void main(){
  String text = 'banana';
  String result = '';
  for(int i=0;i<text.length;i++){
    if(text[i]=='n'){
      result += text[i].toUpperCase();
    }
    else{
      result += text[i];
    }
  }
  print(result);
}
