void main(){
  String text = 'ShO@@1HaN';
  int count = 0;
  for(int i=0;i<text.length;i++){
    if(
      text[i] != text[i].toLowerCase() &&
      text[i] == text[i].toUpperCase()){
      count++;
    };
  }
  print(count);
}
