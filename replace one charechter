void main(){
  String text = 'banana';
  String result = '';
  bool flag = true;
  
  for(int i=0;i<text.length;i++){
    if(text[i]== 'a' && flag){
      result += '*';
      flag = false;
    }
    else{
      result += text[i];
    }
  }
  print(result);
}
